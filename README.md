# CIS2023 Final Project

This project contains a Bash script that counts from 1 to 50.

## How to run
chmod +x count.sh

./count.sh

## Commands Used

git add count.sh
git commit -m "Add count script"
git push origin main
./count.sh
git log --oneline
git status
